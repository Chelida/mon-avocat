import 'package:flutter/material.dart';

class Dossier extends StatelessWidget {
  const Dossier({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Dossier"),backgroundColor: Colors.green,),
    );
  }
}